.page-header
  - @posts.each do |post|
    .jumbotron
      %h1
        = link_to post.title, post
      %p
        = post.body
      %p
        = post.created_at.strftime("%B, %d, %Y")
      %p
        = link_to 'Read More', post, class: 'btn btn-primary'
